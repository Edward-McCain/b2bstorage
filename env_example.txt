APP_NAME=B2BStorage
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Настройки базы данных
# Для локальной разработки используйте LOCAL_DB=true
# Для сервера используйте LOCAL_DB=false
LOCAL_DB=true

# Локальная база данных (для разработки)
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=b2bs_local
DB_USERNAME=b2bs_user
DB_PASSWORD=b2bs_password

# Серверная база данных (для продакшена)
# Эти настройки используются когда LOCAL_DB=false
SERVER_DB_HOST=5.35.85.110
SERVER_DB_PORT=5432
SERVER_DB_DATABASE=b2bstorage
SERVER_DB_USERNAME=b2buser
SERVER_DB_PASSWORD=B2B_Storage_2024!

# Настройки Redis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Настройки почты
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Настройки очередей
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Настройки кэша
CACHE_DRIVER=file
FILESYSTEM_DISK=local

# Настройки API
API_DEBUG=true 